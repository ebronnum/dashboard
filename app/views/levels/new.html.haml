%h2 Choose a type of level to build.

%ul
  %li= link_to "Script and Level builder how to", "https://docs.google.com/a/code.org/document/d/1HcYloRHibxk0Axnuw3A3w_Ht3AEmBHO0IUCaYFfs838/edit?usp=sharing"
  %li= link_to "Build a Maze Level", levels_new_path(type: "Maze")
  %li= link_to "Build an Karel Level", levels_new_path(type: "Karel")
  %li= link_to "Build an Artist Level", levels_new_path(type: "Artist")

%h3 Your Levels
- @levels.each do |level|
  %li
    = level.name
    = link_to t('crud.show'), game_level_path(level.game, level)
    = link_to t('crud.edit'), edit_game_level_path(level.game, level)
    = link_to t('crud.delete'), game_level_path(level.game, level, redirect: levels_new_path), method: :delete

